{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>This is paragraph of the text</p>
                <p>This is paragraph of the text</p>

                <p>This is came from template {{index .StringMap "test"}}</p>

                <p>
                    {{if ne (index .StringMap "remote_ip") ""}}
                        Your IP address is {{index .StringMap "remote_ip"}}
                    {{else}}
                        I cannot get your IP address
                    {{end}}
                </p>
            </div>
        </div>
    </div>
{{end}}